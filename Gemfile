source 'https://rubygems.org'

# The site is built by .github/workflows/pages.yml and deployed as a Pages
# artifact, so we use plain Jekyll rather than the github-pages gem. That also
# keeps the custom plugin in _plugins/ working.
gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.8"
